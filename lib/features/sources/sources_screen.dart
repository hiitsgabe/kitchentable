import 'package:flutter/material.dart';

/// Placeholder so Task 11 can navigate somewhere. Task 12 replaces this whole
/// file with the real screen, so nothing here is meant to last.
class SourcesScreen extends StatelessWidget {
  const SourcesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sources')),
      body: const Center(child: Text('Sources')),
    );
  }
}
